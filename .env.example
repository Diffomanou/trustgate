# FR : Copiez ce fichier en ".env" et remplissez les valeurs.
#      Ne committez JAMAIS le fichier .env dans git.
# EN : Copy this file to ".env" and fill in the values.
#      NEVER commit the .env file to git.

# Optionnel : cle API Gemini pour utiliser GeminiLLMClient au lieu du mock.
# Optional: Gemini API key to use GeminiLLMClient instead of the mock.
# GEMINI_API_KEY=your_key_here
